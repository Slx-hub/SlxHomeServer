# Machine-specific backup configuration.
# Copy this file to backup.env and set the correct device.
# Find the UUID with: blkid /dev/sdX

# Use UUID-based path so the drive is found regardless of which /dev/sdX it lands on.
BACKUP_DEVICE=/dev/disk/by-uuid/YOUR-UUID-HERE
